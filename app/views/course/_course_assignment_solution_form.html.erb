<h2>Submit your solution</h2>
<%= form_for(@assignment_solution,:html =>{:class => 'form-stacked',:multipart => true},:url => 'solve/create' ,:method => :post) do |f| %>
  <% if @assignment_solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment_solution.errors.count, "error") %> prohibited this assignment_solution from being saved:</h2>

      <ul>
      <% @assignment_solution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>
  </br>

  <div class="field">
    <%= f.file_field :file %>
  </div>
  </br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

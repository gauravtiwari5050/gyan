<h2> Select a teacher for this course </h2>
<%= form_for(@course_allocation,:html =>{:class => 'form-stacked',:multipart => true},:url => 'create' ,:method => :put) do |f| %>
  <% if @course_allocation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_allocation.errors.count, "error") %> prohibited this course_allocation from being saved:</h2>

      <ul>
      <% @course_allocation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.select :user_id ,options_for_select(@teachers) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
